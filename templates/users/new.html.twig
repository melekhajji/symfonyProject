

{% extends 'back.html.twig' %}
{% block container %}

        <main class="content">
            <div class="container-fluid p-0">

                <h1 class="h3 mb-3">Ajouter personnel</h1>

                <div class="row">
                    <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-striped table-hover">

    {{ include('users/_form.html.twig') }}

    <a href="{{ path('users_index') }}"> Retourner à la liste</a>

                                    </table>
                                </div></div></div></div></div></div></main>
{% endblock %}
