{% extends 'back.html.twig' %}

{% block title %}Réclamation {% endblock %}

{% block container %}

    <section class="tables">
        <div class="container-fluid">
            <div class="row">


                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-close">
                            <div class="dropdown">
                                <button type="button" id="closeCard3" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="dropdown-toggle"><i class="fa fa-ellipsis-v"></i></button>
                                <div aria-labelledby="closeCard3" class="dropdown-menu dropdown-menu-right has-shadow"><a href="#" class="dropdown-item remove"> <i class="fa fa-times"></i>Close</a><a href="#" class="dropdown-item edit"> <i class="fa fa-gear"></i>Edit</a></div>
                            </div>
                        </div>
                        <div class="card-header d-flex align-items-center">
                            <h3 class="h4">Listes des Reclamations</h3>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-hover">
                                    <thead>

                                    <tr>
                                        <td>Id</td>
                                        <td>Commentaire</td>
                                        <td>Type</td>
                                        <td>Created_at</td>
                                        <td>Etat</td>
                                        <td>User</td>
                                        <td>Action</td>

                                    </tr>

                                    </thead>
                                    <tbody>
                                    {% for rec in reclamations %}
                                        <tr>
                                            <td>{{ rec.id }}</td>
                                            <td>{{ rec.type }}</td>
                                            <td>{{ rec.createdAt|date }}</td>
                                            <td>{{ rec.etat }}</td>
                                            <td>{{ rec.user }}</td>
                                            <td><a href="{{ path('updateReclamation',{'id':rec.id}) }}">Update</a>
                                                <a href="{{ path('deleteReclamation',{'id':rec.id}) }}">Delete</a>
                                            </td>
                                        </tr>

                                    {% else %}
                                        <tr>
                                            <td colspan="7">no records found</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                                <a href="{{ path('addReclamation') }}">Create new</a>
                            </div>
                        </div>
                    </div>
                </div>


            </div>
        </div>
    </section>

    </div>
    </div>
    </div>
    </body>
{% endblock %}